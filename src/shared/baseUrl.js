export const baseUrl = 'http://my-server-kitchen.herokuapp.com/';
// export const baseUrl ='http://localhost:3001/';

// if the herokuapp server stops working
// uncomment the local host and do the following!!

// npm install -g json-server
// and to run the server use the command

// --> npx json-server --watch db.json -p 3001 -d 2000
